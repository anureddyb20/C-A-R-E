{
  "name": "care-healthcare-system",
  "version": "1.0.0",
  "private": true,
  "description": "C.A.R.E. Healthcare Monitoring System",
  "scripts": {
    "install": "cd web_monitor/frontend && npm install",
    "build": "cd web_monitor/frontend && npm run build",
    "dev": "cd web_monitor/frontend && npm run build && cd ../backend && (test -d venv || python3 -m venv venv) && source venv/bin/activate && pip install -q -r requirements.txt && uvicorn main:app --reload --port 8000",
    "start": "cd web_monitor/frontend && npm run build && cd ../backend && (test -d venv || python3 -m venv venv) && source venv/bin/activate && pip install -q -r requirements.txt && uvicorn main:app --reload --port 8000"
  }
}
